---
import "../styles/global.css";

const B = import.meta.env.BASE_URL;
const page = {
  title: "Now | Sivan Syed",
  description: "What I'm focused on right now — robotics, controls, physics, and systems.",
};
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{page.title}</title>
    <meta name="description" content={page.description} />
    <link rel="icon" href={`${B}favicon.svg`} />
  </head>
  <body class="min-h-screen overflow-x-hidden bg-gradient-to-b from-black via-zinc-950 to-black text-zinc-100 antialiased selection:bg-indigo-500/20">
    {/* Decorative background */}
    <div aria-hidden class="pointer-events-none fixed inset-0 -z-20">
      <div class="absolute inset-0 [mask-image:radial-gradient(70%_50%_at_50%_0%,black,transparent)]">
        <div class="absolute inset-0 opacity-[0.04] [background-image:linear-gradient(to_right,theme(colors.zinc.400/20)_1px,transparent_1px),linear-gradient(to_bottom,theme(colors.zinc.400/20)_1px,transparent_1px)] [background-size:40px_40px] [background-position:center]"></div>
        <div class="absolute -top-24 left-1/2 h-[36rem] w-[72rem] -translate-x-1/2 rounded-full bg-gradient-to-r from-zinc-200/10 via-indigo-400/15 to-zinc-200/10 blur-3xl"></div>
      </div>
    </div>

    {/* Header (shared style) */}
    <header class="sticky top-0 z-50 backdrop-blur supports-[backdrop-filter]:bg-zinc-950/55 border-b border-zinc-800/60">
      <div class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-6 md:px-12 lg:px-20 py-4">
        <a href={`${B}`} class="group flex items-center gap-3">
          <img src={`${B}favicon.svg`} alt="Logo" class="size-9 transition-transform duration-300 group-hover:rotate-6" />
          <div>
            <h1 class="text-2xl font-semibold tracking-tight">Sivan Syed</h1>
            <p class="text-xs text-zinc-400">Physics & ECE • Robotics • Controls • Systems</p>
          </div>
        </a>
        <nav class="flex items-center gap-2 text-sm">
          <a class="navlink" href={`${B}`}>Home</a>
          <a class="navlink" href={`${B}projects`}>Projects</a>
          <a class="navlink" href={`${B}tutoring`}>Tutoring</a>
          <a class="navlink" href={`/SyedS_ResumeELC_Print.pdf`} target="_blank">Resume</a>
        </nav>
      </div>
    </header>

    {/* Main */}
    <main class="mx-auto max-w-[90rem] px-6 md:px-12 lg:px-20 py-10 md:py-14 space-y-24">
      {/* Hero */}
      <section class="mx-auto max-w-6xl">
        <p class="text-xs uppercase tracking-[0.2em] text-zinc-400/90">Now • Focus • Current priorities</p>
        <h2 class="mt-3 text-4xl font-bold leading-tight md:text-5xl">
          <span class="bg-gradient-to-r from-zinc-100 via-zinc-400 to-zinc-100 bg-clip-text text-transparent [text-shadow:0_0_0.5px_rgba(255,255,255,0.35)] animate-shimmer">What I'm focused on right now</span>
        </h2>
        <p class="mt-4 max-w-3xl text-lg text-zinc-300/90">
          Snapshot of work in progress across robotics, controls, device physics, and systems.
        </p>
        <div class="mt-4 flex flex-wrap items-center gap-3 text-xs text-zinc-400/90">
          <span>Last updated: {new Date().toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' })}</span>
          <span class="opacity-50">•</span>
          <span>Location: Austin, TX</span>
        </div>
      </section>

      <hr class="section-divider-glow" />

      {/* Focus list */}
      <section class="section">
        <h3 class="section-title">Active tracks</h3>
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2">
          <div class="card">
            <div class="card-border"></div>
            <h4 class="font-medium">Core priorities</h4>
            <ul class="mt-3 list-disc space-y-2 pl-5 text-sm text-zinc-300">
              <li>Finishing portfolio site with Astro + Svelte + FastAPI</li>
              <li>Developing the Ping Pong Balancer Robot</li>
              <li>Contributing to LEGEND research via MaGe simulations of WLS fibers with Professor Karol Lang</li>
              <li>Researching wafer growth with controlled Metal-Organic Chemical Vapor Deposition (MOCVD) with Professor Sanjay Banerjee</li>
              <li>Creating a custom boost converter using the C2000 and self-designed and built inductor</li>
            </ul>
          </div>
          <div class="card">
            <div class="card-border"></div>
            <h4 class="font-medium">What this looks like week-to-week</h4>
            <ul class="mt-3 space-y-2 text-sm text-zinc-300">
              <li>⏱️ 2–3 code pushes/week to the portfolio (content + components)</li>
              <li>🧪 Controls experiments on the balancer: PID tuning & camera loop</li>
              <li>🧮 Device/AMO reading hours; summarize to notes & diagrams</li>
              <li>🔧 Power electronics prototyping & thermal checks</li>
            </ul>
          </div>
        </div>
      </section>

      <hr class="section-divider-glow" />

      {/* Light timeline */}
      <section class="section">
        <h3 class="section-title">Recent milestones</h3>
        <div class="space-y-3">
          {[
            { d: "This week", t: "Homepage & tutoring pages redesigned (Astro + Tailwind)." },
            { d: "Last week", t: "Closed-loop test on Ping Pong Balancer; initial results logged." },
            { d: "Earlier", t: "Ran MaGe batch for WLS fiber scenarios; documented outputs." },
          ].map((m) => (
            <div class="relative pl-6">
              <div class="absolute left-0 top-2 size-2 rounded-full bg-indigo-400/60 shadow-[0_0_12px_rgba(99,102,241,0.35)]"></div>
              <div class="text-sm text-zinc-400">{m.d}</div>
              <div class="text-sm text-zinc-200">{m.t}</div>
            </div>
          ))}
        </div>
      </section>

      <hr class="section-divider-glow" />

      {/* Contact CTA */}
      <section class="section">
        <div class="card">
          <div class="card-border"></div>
          <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
            <div>
              <h3 class="text-lg font-semibold">Want details or collab?</h3>
              <p class="mt-1 text-sm text-zinc-300">Happy to share notes, schematics, or references for any of the above.</p>
            </div>
            <div class="flex gap-3">
              <a href={`${B}tutoring#contact`} class="btn-ghost">Message me</a>
              <a href={`${B}projects`} class="btn-primary">Browse projects</a>
            </div>
          </div>
        </div>
      </section>
    </main>

    <footer class="mx-auto max-w-7xl px-6 md:px-12 lg:px-20 pb-10 pt-6 text-sm text-zinc-500/90">
      <div class="flex items-center justify-between border-t border-zinc-800/70 pt-4">
        <p>© {new Date().getFullYear()} Sivan Syed</p>
        <a class="underline decoration-zinc-400/30 underline-offset-2 hover:decoration-zinc-200/60" href={`${B}`}>Back to home</a>
      </div>
    </footer>
  </body>
</html>
